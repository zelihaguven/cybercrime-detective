services:
  - type: web
    name: detective-game
    env: node
    buildCommand: npm install && npm run build && cd server && npm install
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: BASE_PATH
        value: /
