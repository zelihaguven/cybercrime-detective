services:
  - type: web
    name: detective-game
    env: node
    buildCommand: npm install && npm --prefix server install && npm run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20"
